package collectionsFramerors;

public class Descriptions {
    /*
    Java Colllection Framerork provides lots of interfaces lots of classes which are already
    inbuilt under JDK package under Java, they provide various data structure ,implementation and
    already inbuilt algorithm also, we don't need to write algorithm and data structure again
    and again, let see i need to sort data, iterate data for that I really don't need to write
    whole program for that , simply use interface or classes, or method implementation to solve my
    query, purpose quickly.

    let see I need to store data order base and data is dynamic in this case I need to user
    ArrayList , If I want to store my data into key and value pair format, in that case
    I will go with the Map interface or HashMap
    =========================Collection================================================
    Collection(i)-- extends--set(i)---implements---HashSet(c)
                                   ---implements---LinkedHashSet(c)
                                   ---extends --SortedSet(i)---implements---TreeSet(c)

    Collection(i)--extends--list(i)---implements---ArrayList(c)
                                  ---implements---Vector(c)
                                  ---implements---LinkedList(c)

    Collection(i)--extends--queue(i)---implements---LinkedList(c)
                                    ---implements---PriorityQueue(c)

    ==========================Map========================================================

    Map(i)---implements---HashTable(c)
          ---implements---LinkedHashMap(c)
          ---implements---HashMap(c)
          --extends--SortedMap(i)---implements---TreeMap(c)

    ==============Object=============================================================
    Object(c)--extends--Arrays(c)
             --extends--Collections(c)
     */
}
